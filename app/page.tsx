'use client';
import {useEffect,useMemo,useState} from 'react';
import {supabase} from '../lib/supabase';
const ADMIN_EMAIL='suhailahmedaamro786@gmail.com';
type Student={id:string;student_id:string;full_name:string;father_name:string|null;guardian_name:string|null;photo_url:string|null;class_id:string|null;active:boolean;classes?:{name:string;section:string}|null};
type ClassRow={id:string;name:string;section:string;academic_year:string|null};
type Parent={id:string;full_name:string;phone:string|null;approved:boolean};
export default function Home(){const[user,setUser]=useState<any>(null);const[loading,setLoading]=useState(true);useEffect(()=>{supabase().auth.getUser().then(({data})=>{setUser(data.user);setLoading(false)})},[]);if(loading)return <main className="wrap"><div className="card">Loading admin dashboard...</div></main>;if(!user)return <Login/>;if((user.email||'').toLowerCase()!==ADMIN_EMAIL)return <AccessDenied/>;return <Admin user={user}/>}
function Admin({user}:{user:any}){const[tab,setTab]=useState<'students'|'parents'|'classes'>('students');const[students,setStudents]=useState<Student[]>([]);const[parents,setParents]=useState<Parent[]>([]);const[classes,setClasses]=useState<ClassRow[]>([]);const[stats,setStats]=useState({students:0,parents:0,pending:0,announcements:0});const[busy,setBusy]=useState(true);const[error,setError]=useState('');const[query,setQuery]=useState('');const[showStudent,setShowStudent]=useState(false);const[editing,setEditing]=useState<Student|null>(null);const[showClass,setShowClass]=useState(false);const[showParent,setShowParent]=useState(false);
 const load=async()=>{setBusy(true);setError('');const s=supabase();const[{data:st,error:se},{data:cl,error:ce},{data:pa,error:pe},{count:rc},{count:ac}]=await Promise.all([s.from('students').select('id,student_id,full_name,father_name,guardian_name,photo_url,class_id,active,classes(name,section)').order('full_name'),s.from('classes').select('id,name,section,academic_year').order('name').order('section'),s.from('parents').select('id,full_name,phone,approved').order('full_name'),s.from('result_documents').select('id',{count:'exact',head:true}).in('status',['pending','processing','review']),s.from('announcements').select('id',{count:'exact',head:true})]);if(se||ce||pe)setError(se?.message||ce?.message||pe?.message||'Could not load data.');setStudents((st||[]) as Student[]);setClasses(cl||[]);setParents((pa||[]) as Parent[]);setStats({students:st?.length||0,parents:pa?.length||0,pending:rc||0,announcements:ac||0});setBusy(false)};useEffect(()=>{load()},[]);
 const filtered=useMemo(()=>students.filter(x=>`${x.full_name} ${x.student_id} ${x.father_name||''}`.toLowerCase().includes(query.toLowerCase())),[students,query]);
 async function deactivate(id:string){if(!confirm('Deactivate this student?'))return;const{error}=await supabase().from('students').update({active:false}).eq('id',id);if(error)setError(error.message);else load()}
 async function toggleParent(p:Parent){const{error}=await supabase().from('parents').update({approved:!p.approved}).eq('id',p.id);if(error)setError(error.message);else load()}
 return <main className="wrap"><div className="nav"><div><div className="brand">🛡️ NPSD Admin Dashboard</div><div className="muted">Noble Public School Dadu</div><div className="muted">Admin: {user.email}</div></div><button className="btn secondary" onClick={()=>supabase().auth.signOut().then(()=>location.reload())}>Logout</button></div><div className="grid stats"><Stat t="Students" v={stats.students}/><Stat t="Parents" v={stats.parents}/><Stat t="Pending Results" v={stats.pending}/><Stat t="Announcements" v={stats.announcements}/></div>{error&&<div className="alert">{error}</div>}<div className="tabs"><button className={tab==='students'?'tab active':'tab'} onClick={()=>setTab('students')}>👨‍🎓 Students</button><button className={tab==='parents'?'tab active':'tab'} onClick={()=>setTab('parents')}>👨‍👩‍👧 Parents</button><button className={tab==='classes'?'tab active':'tab'} onClick={()=>setTab('classes')}>🏫 Classes</button></div>
 {tab==='students'&&<section className="card section"><div className="sectionHead"><div><h2>Students</h2><p className="muted">Create, edit and deactivate student records.</p></div><button className="btn" onClick={()=>{setEditing(null);setShowStudent(true)}}>＋ Add Student</button></div><div className="toolbar"><input className="input search" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search name, Student ID or father name..."/><button className="btn secondary" onClick={load} disabled={busy}>{busy?'Loading...':'Refresh'}</button></div><div className="tableWrap"><table><thead><tr><th>Student</th><th>ID</th><th>Class</th><th>Father / Guardian</th><th>Status</th><th>Actions</th></tr></thead><tbody>{filtered.map(st=><tr key={st.id}><td><b>{st.full_name}</b></td><td>{st.student_id}</td><td>{st.classes?`${st.classes.name} — ${st.classes.section}`:'Unassigned'}</td><td>{st.father_name||st.guardian_name||'—'}</td><td><span className={`badge ${st.active?'ok':'off'}`}>{st.active?'Active':'Inactive'}</span></td><td><button className="linkBtn" onClick={()=>{setEditing(st);setShowStudent(true)}}>Edit</button>{st.active&&<button className="linkBtn danger" onClick={()=>deactivate(st.id)}>Deactivate</button>}</td></tr>)}{!filtered.length&&<tr><td colSpan={6} className="empty">No students found.</td></tr>}</tbody></table></div></section>}
 {tab==='parents'&&<section className="card section"><div className="sectionHead"><div><h2>Parents</h2><p className="muted">Approve parent accounts before portal access.</p></div><button className="btn" onClick={()=>setShowParent(true)}>＋ Add Parent</button></div><div className="tableWrap"><table><thead><tr><th>Name</th><th>Phone</th><th>Approval</th><th>Action</th></tr></thead><tbody>{parents.map(p=><tr key={p.id}><td><b>{p.full_name}</b></td><td>{p.phone||'—'}</td><td><span className={`badge ${p.approved?'ok':'off'}`}>{p.approved?'Approved':'Pending'}</span></td><td><button className="linkBtn" onClick={()=>toggleParent(p)}>{p.approved?'Revoke':'Approve'}</button></td></tr>)}{!parents.length&&<tr><td colSpan={4} className="empty">No parents found.</td></tr>}</tbody></table></div></section>}
 {tab==='classes'&&<section className="card section"><div className="sectionHead"><div><h2>Classes & Sections</h2><p className="muted">Create the classes used by student records.</p></div><button className="btn" onClick={()=>setShowClass(true)}>＋ Add Class</button></div><div className="tableWrap"><table><thead><tr><th>Class</th><th>Section</th><th>Academic Year</th></tr></thead><tbody>{classes.map(c=><tr key={c.id}><td><b>{c.name}</b></td><td>{c.section}</td><td>{c.academic_year||'—'}</td></tr>)}{!classes.length&&<tr><td colSpan={3} className="empty">No classes found.</td></tr>}</tbody></table></div></section>}
 <section className="grid section modules"><Module n="Attendance" d="Daily, monthly and percentage tracking"/><Module n="Exams & Results" d="Marks, grades, position and publishing"/><Module n="Result Review" d="PDF import and manual review queue"/><Module n="Announcements" d="Publish school notices"/><Module n="Digital Cards" d="Student card and QR verification"/><Module n="Audit Logs" d="Track administrator actions"/></section>{showStudent&&<StudentForm classes={classes} student={editing} onClose={()=>setShowStudent(false)} onSaved={()=>{setShowStudent(false);load()}}/>}{showClass&&<ClassForm onClose={()=>setShowClass(false)} onSaved={()=>{setShowClass(false);load()}}/>}{showParent&&<ParentForm onClose={()=>setShowParent(false)} onSaved={()=>{setShowParent(false);load()}}/>}</main>}
function StudentForm({classes,student,onClose,onSaved}:{classes:ClassRow[];student:Student|null;onClose:()=>void;onSaved:()=>void}){const[form,setForm]=useState({student_id:student?.student_id||'',full_name:student?.full_name||'',father_name:student?.father_name||'',guardian_name:student?.guardian_name||'',class_id:student?.class_id||'',photo_url:student?.photo_url||'',active:student?.active??true});const[saving,setSaving]=useState(false);const[error,setError]=useState('');const set=(k:string,v:any)=>setForm(x=>({...x,[k]:v}));async function save(e:any){e.preventDefault();setSaving(true);setError('');const payload={...form,father_name:form.father_name||null,guardian_name:form.guardian_name||null,class_id:form.class_id||null,photo_url:form.photo_url||null};const{error}=student?await supabase().from('students').update(payload).eq('id',student.id):await supabase().from('students').insert(payload);setSaving(false);if(error)setError(error.message);else onSaved()}return <Modal title={student?'Edit Student':'Add Student'} close={onClose}><form onSubmit={save}><label>Student ID / Roll No<input className="input" required value={form.student_id} onChange={e=>set('student_id',e.target.value)}/></label><label>Full Name<input className="input" required value={form.full_name} onChange={e=>set('full_name',e.target.value)}/></label><div className="two"><label>Father Name<input className="input" value={form.father_name} onChange={e=>set('father_name',e.target.value)}/></label><label>Guardian Name<input className="input" value={form.guardian_name} onChange={e=>set('guardian_name',e.target.value)}/></label></div><label>Class / Section<select className="input" value={form.class_id} onChange={e=>set('class_id',e.target.value)}><option value="">Unassigned</option>{classes.map(c=><option key={c.id} value={c.id}>{c.name} — {c.section}{c.academic_year?` (${c.academic_year})`:''}</option>)}</select></label><label>Photo URL<input className="input" value={form.photo_url} onChange={e=>set('photo_url',e.target.value)} placeholder="Optional"/></label>{student&&<label className="check"><input type="checkbox" checked={form.active} onChange={e=>set('active',e.target.checked)}/> Active</label>}{error&&<div className="alert">{error}</div>}<div className="actions"><button type="button" className="btn secondary" onClick={onClose}>Cancel</button><button className="btn" disabled={saving}>{saving?'Saving...':student?'Save Changes':'Create Student'}</button></div></form></Modal>}
function ClassForm({onClose,onSaved}:{onClose:()=>void;onSaved:()=>void}){const[f,setF]=useState({name:'',section:'',academic_year:''});const[err,setErr]=useState('');async function save(e:any){e.preventDefault();const{error}=await supabase().from('classes').insert({name:f.name.trim(),section:f.section.trim(),academic_year:f.academic_year.trim()||null});if(error)setErr(error.message);else onSaved()}return <Modal title="Add Class / Section" close={onClose}><form onSubmit={save}><label>Class<input className="input" required value={f.name} onChange={e=>setF({...f,name:e.target.value})} placeholder="e.g. Grade 8"/></label><label>Section<input className="input" required value={f.section} onChange={e=>setF({...f,section:e.target.value})} placeholder="e.g. A"/></label><label>Academic Year<input className="input" value={f.academic_year} onChange={e=>setF({...f,academic_year:e.target.value})} placeholder="e.g. 2026-27"/></label>{err&&<div className="alert">{err}</div>}<div className="actions"><button type="button" className="btn secondary" onClick={onClose}>Cancel</button><button className="btn">Create Class</button></div></form></Modal>}
function ParentForm({onClose,onSaved}:{onClose:()=>void;onSaved:()=>void}){const[f,setF]=useState({full_name:'',phone:''});const[err,setErr]=useState('');async function save(e:any){e.preventDefault();const{error}=await supabase().from('parents').insert({full_name:f.full_name.trim(),phone:f.phone.trim()||null,approved:false});if(error)setErr(error.message);else onSaved()}return <Modal title="Add Parent" close={onClose}><form onSubmit={save}><label>Parent Name<input className="input" required value={f.full_name} onChange={e=>setF({...f,full_name:e.target.value})}/></label><label>Phone<input className="input" value={f.phone} onChange={e=>setF({...f,phone:e.target.value})}/></label><p className="muted">New parent accounts start as Pending until approved.</p>{err&&<div className="alert">{err}</div>}<div className="actions"><button type="button" className="btn secondary" onClick={onClose}>Cancel</button><button className="btn">Create Parent</button></div></form></Modal>}
function Modal({title,close,children}:{title:string;close:()=>void;children:any}){return <div className="modal"><div className="modalCard"><div className="sectionHead"><h2>{title}</h2><button className="iconBtn" onClick={close}>×</button></div>{children}</div></div>}
function Stat({t,v}:{t:string;v:number}){return <div className="card"><div className="muted">{t}</div><div className="stat">{v}</div></div>}
function Module({n,d}:{n:string;d:string}){return <div className="card module"><b>{n}</b><p className="muted">{d}</p><span className="badge">Coming next</span></div>}
function Login(){const[e,setE]=useState('');const[p,setP]=useState('');const[x,setX]=useState('');const[busy,setBusy]=useState(false);async function go(a:any){a.preventDefault();setX('');if(e.trim().toLowerCase()!==ADMIN_EMAIL){setX('Access Denied: This account is not authorized.');return}setBusy(true);const{error}=await supabase().auth.signInWithPassword({email:ADMIN_EMAIL,password:p});setBusy(false);if(error)setX('Access Denied: Invalid admin credentials.');else location.reload()}async function google(){setX('');setBusy(true);const{error}=await supabase().auth.signInWithOAuth({provider:'google',options:{redirectTo:`${window.location.origin}/auth/callback`}});if(error){setBusy(false);setX(error.message)}}return <main className="wrap"><div className="card login"><h1>NPSD Admin</h1><p className="muted">Authorized administrator only</p><form onSubmit={go}><label>Admin Email</label><input className="input" type="email" required value={e} onChange={a=>setE(a.target.value)} placeholder={ADMIN_EMAIL}/><label>Password</label><input className="input" type="password" required value={p} onChange={a=>setP(a.target.value)}/>{x&&<p>{x}</p>}<button className="btn" disabled={busy}>{busy?'Signing in...':'Sign in'}</button></form><div className="section"><button className="btn full" type="button" disabled={busy} onClick={google}>Continue with Google</button><p className="muted">Google sign-in is checked against the authorized admin email after authentication.</p></div></div></main>}
function AccessDenied(){useEffect(()=>{supabase().auth.signOut()},[]);return <main className="wrap"><div className="card login"><h1>Access Denied</h1><p className="muted">This account is not authorized to use the NPSD Admin Dashboard.</p></div></main>}
